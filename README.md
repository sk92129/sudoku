# this game came from an open source repo

## what I did to it:

  Converted it to a maven build project

  Fix the java compile errors

  Run 

How to build and run:

mvn clean compile exec:java -Dexec.mainClass="sudoku.Main"
